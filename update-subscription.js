// Simple script to update subscription tiers
// This will be run manually to fix subscription tier issues

console.log('This script needs to be run with proper database access');
console.log('Please provide the email address of the user to update to Pro tier');

// Example of what needs to be done:
// UPDATE users SET subscription_tier = 'pro' WHERE email = 'user@example.com';